<%= form_for([@project, @product_breakdown_structure], html: { class: 'form-horizontal pbs' }) do |f| %>
  <% if @product_breakdown_structure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_breakdown_structure.errors.count, "error") %> prohibited this product_breakdown_structure from being saved:</h2>

      <ul>
      <% @product_breakdown_structure.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <input type="hidden" id="id" name="id" />
  
  <div class="form-group">
    <%= f.label :name, class: 'col-md-3 control-label' %>
    <div class="col-md-9">
      <%= f.text_field :name, {class: 'form-control', placeholder: 'Name'} %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :parent, class: 'col-md-3 control-label' %>
    <div class="col-md-9">
      <%= select_tag :parent, options_from_collection_for_select(ProductBreakdownStructure.order_for_selects(params[:project_id]), "id", :name_for_selects), { class: 'form-control' } %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-3 col-sm-9">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>